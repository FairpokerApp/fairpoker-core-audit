<!doctype html>
<html lang="zh" data-lang="zh">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Fair Poker — 安全与攻击模型</title>
    <meta name="description" content="Fair Poker security model: how operator card-control, hole-card peeking, tampering, and result rewriting are placed inside a verifiable evidence boundary." />
    <meta name="theme-color" content="#07130F" />
    <style>
      :root{
        --fp-bg:#07130F;--fp-card:#0F2418;--fp-emerald:#1FA06A;--fp-emerald-soft:rgba(31,160,106,.14);
        --fp-gold:#E0C06A;--fp-gold-soft:rgba(224,192,106,.12);--fp-ink:#F2EFE6;--fp-muted:#9BB0A4;
        --fp-body:#D7E2DB;--fp-line:rgba(159,176,168,.16);--fp-radius:14px;--fp-maxw:1040px;
        --fp-sans:-apple-system,BlinkMacSystemFont,"Segoe UI","Noto Sans SC",system-ui,Arial,sans-serif;
        --fp-mono:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,monospace;
      }
      *{box-sizing:border-box}
      html{color-scheme:dark;scroll-behavior:smooth}
      body{margin:0;background:var(--fp-bg);color:var(--fp-ink);font-family:var(--fp-sans);line-height:1.72;-webkit-font-smoothing:antialiased}
      a{color:var(--fp-gold);text-decoration:none;font-weight:600}
      a:hover{text-decoration:underline}
      .fp-nav{position:sticky;top:0;z-index:10;display:flex;align-items:center;justify-content:space-between;gap:16px;padding:13px clamp(16px,5vw,40px);background:rgba(7,19,15,.82);backdrop-filter:saturate(140%) blur(12px);-webkit-backdrop-filter:saturate(140%) blur(12px);border-bottom:1px solid var(--fp-line)}
      .fp-brand{display:flex;align-items:center;gap:10px;font-weight:700;color:var(--fp-ink);font-size:16px}
      .fp-mark{width:30px;height:30px;border-radius:8px;background:var(--fp-gold);color:#241a06;display:flex;align-items:center;justify-content:center;font-size:17px}
      .fp-nav-right{display:flex;align-items:center;gap:14px;flex-wrap:wrap}
      .fp-nav-links{display:flex;align-items:center;gap:2px;flex-wrap:wrap}
      .fp-nav-links a{color:var(--fp-muted);font-weight:500;font-size:14px;padding:7px 11px;border-radius:8px}
      .fp-nav-links a:hover{color:var(--fp-ink);background:rgba(255,255,255,.05);text-decoration:none}
      .fp-langs{display:flex;gap:4px}
      .fp-langs button{font:inherit;font-size:13px;color:var(--fp-muted);background:transparent;border:1px solid var(--fp-line);border-radius:999px;padding:5px 11px;cursor:pointer}
      .fp-langs button[aria-pressed="true"]{color:#241a06;background:var(--fp-gold);border-color:var(--fp-gold);font-weight:600}
      main{max-width:var(--fp-maxw);margin:0 auto;padding:clamp(28px,6vw,56px) clamp(16px,5vw,40px) 72px}
      .fp-eyebrow{display:inline-block;font-size:12px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--fp-emerald)}
      h1{font-size:clamp(31px,6vw,50px);line-height:1.08;letter-spacing:-.02em;margin:14px 0 16px;font-weight:700}
      h2{font-size:23px;margin:42px 0 12px;font-weight:700;letter-spacing:-.01em}
      h3{font-size:18px;margin:30px 0 12px;font-weight:600}
      p,li{color:var(--fp-body);font-size:16px}
      .fp-lead{font-size:18px;color:var(--fp-muted);max-width:66ch;line-height:1.7}
      .fp-callout{border:1px solid var(--fp-line);border-left:3px solid var(--fp-emerald);background:var(--fp-emerald-soft);border-radius:12px;padding:15px 18px;margin:20px 0}
      .fp-callout p{margin:0 0 8px}.fp-callout p:last-child{margin:0}
      dl.fp-dl{display:grid;grid-template-columns:185px minmax(0,1fr);gap:1px;margin:14px 0;background:var(--fp-line);border:1px solid var(--fp-line);border-radius:12px;overflow:hidden}
      dl.fp-dl dt{background:var(--fp-card);padding:13px 16px;font-size:12.5px;font-weight:700;color:var(--fp-muted)}
      dl.fp-dl dd{background:var(--fp-card);padding:13px 16px;margin:0;min-width:0;overflow-wrap:anywhere;font-family:var(--fp-mono);font-size:12.5px;color:var(--fp-ink)}
      .fp-table-wrap{border:1px solid var(--fp-line);border-radius:var(--fp-radius);overflow:hidden;margin:16px 0;background:var(--fp-card)}
      table{width:100%;border-collapse:collapse}
      th,td{text-align:left;vertical-align:top;padding:13px 16px;border-bottom:1px solid var(--fp-line)}
      tr:last-child td{border-bottom:none}
      th{font-size:11.5px;letter-spacing:.06em;text-transform:uppercase;color:var(--fp-muted);font-weight:700;background:rgba(255,255,255,.02)}
      td{font-size:14px;color:#CBD8D0;line-height:1.6}
      td:first-child{color:var(--fp-ink);font-weight:600}
      ul.fp-tips{list-style:none;margin:14px 0;padding:0;display:grid;gap:10px}
      ul.fp-tips li{position:relative;padding:12px 16px 12px 40px;background:var(--fp-card);border:1px solid var(--fp-line);border-radius:12px;font-size:15px;color:#CBD8D0}
      ul.fp-tips li::before{content:"";position:absolute;left:15px;top:16px;width:13px;height:7px;border-left:2px solid var(--fp-emerald);border-bottom:2px solid var(--fp-emerald);transform:rotate(-45deg)}
      code{font-family:var(--fp-mono);font-size:.9em;background:rgba(159,176,168,.12);border:1px solid var(--fp-line);border-radius:6px;padding:1px 6px;overflow-wrap:anywhere}
      .fp-foot{border-top:1px solid var(--fp-line);max-width:var(--fp-maxw);margin:0 auto;padding:26px clamp(16px,5vw,40px) 52px;color:var(--fp-muted);font-size:13.5px;display:flex;flex-wrap:wrap;gap:12px 26px;justify-content:space-between;align-items:center}
      .fp-foot a{color:var(--fp-muted);font-weight:500}.fp-foot a:hover{color:var(--fp-ink)}
      .fp-foot-links{display:flex;flex-wrap:wrap;gap:16px}
      [data-l]{display:none}
      html[data-lang="zh"] [data-l="zh"],html[data-lang="en"] [data-l="en"]{display:revert}
      @media (max-width:820px){dl.fp-dl{grid-template-columns:1fr}.fp-nav-links{display:none}
        table,thead,tbody,tr,th,td{display:block}th{display:none}
        td{border-bottom:0;padding:4px 16px}td:first-child{padding-top:14px;font-size:15px}
        tr{border-bottom:1px solid var(--fp-line);padding:8px 0}tr:last-child{border-bottom:none}}
    </style>
  </head>
  <body>
    <nav class="fp-nav">
      <a class="fp-brand" href="https://fairpoker.app/"><span class="fp-mark" aria-hidden="true">&#9824;</span>Fair Poker</a>
      <div class="fp-nav-right">
        <div class="fp-nav-links">
          <a href="https://fairpoker.app/"><span data-l="zh">首页</span><span data-l="en">Home</span></a>
          <a href="https://fairpoker.app/audit-report.html"><span data-l="zh">公平证据</span><span data-l="en">Evidence</span></a>
          <a href="https://fairpoker.app/verify-guide.html"><span data-l="zh">复验教程</span><span data-l="en">Verify</span></a>
          <a href="https://fairpoker.app/security.html"><span data-l="zh">安全</span><span data-l="en">Security</span></a>
        </div>
        <div class="fp-langs" id="fp-langs" aria-label="Language">
          <button type="button" data-set="zh">中文</button>
          <button type="button" data-set="en">EN</button>
        </div>
      </div>
    </nav>

    <main>
      <div data-l="zh">
        <span class="fp-eyebrow">安全模型 · Security</span>
        <h1>安全与攻击模型</h1>
        <p class="fp-lead">这一页说明 Fair Poker 如何把"平台控牌、偷看底牌、篡改牌局、事后改记录"放进可验证的证据边界：发牌在玩家浏览器的协议中形成，中继只转发消息，事件进入签名 hash-chain transcript，并可由本地 verifier 重放。</p>
      </div>
      <div data-l="en">
        <span class="fp-eyebrow">Security</span>
        <h1>Security and attack model</h1>
        <p class="fp-lead">This page explains how Fair Poker places operator card-control, hole-card peeking, tampering, and after-the-fact rewriting inside a verifiable evidence boundary: dealing is formed in the player browser protocol, the relay only forwards messages, events enter a signed hash-chain transcript, and any of it can be replayed by the local verifier.</p>
      </div>

      <section data-release-facts>
        <h2><span data-l="zh">发布事实</span><span data-l="en">Release facts</span></h2>
        <dl class="fp-dl">
          <dt>Official domain</dt><dd>https://fairpoker.app</dd>
          <dt>Game client CID</dt><dd><span data-release-field="gameClientCid">Read from canonical release JSON</span></dd>
          <dt>Source package CID</dt><dd><span data-release-field="sourceCid">Read from canonical release JSON</span></dd>
          <dt>Source fingerprint</dt><dd><span data-release-field="sourceFingerprint">Read from canonical release JSON</span></dd>
        </dl>
        <div class="fp-callout">
          <p data-l="zh"><strong>权威发布身份：</strong>请用牌局客户端 CID、源码包 CID、源码指纹和压缩包 SHA256 对照 <a href="./source/release.json">source/release.json</a> 与 <a href="./ai.json">ai.json</a>。若缓存页面、镜像或 AI/搜索摘要显示另一组组合，应视为过期副本。</p>
          <p data-l="zh"><strong>官方控牌边界：</strong>在已公布的 Game client CID 与可本地复验的 transcript 下，官方/中继无法在保持 verifier 通过的前提下伪造或篡改签名事件、指定某玩家拿到特定牌、偷看未公开底牌或事后改赢家。私有发牌消息端到端加密发给收牌方，中继只见密文、拿不到逐卡解密钥。这是"对照可验证产物"的主张，不是"相信隐藏服务器或外部徽章"。</p>
          <p data-l="en"><strong>Authoritative release identity:</strong> compare the Game client CID, source package CID, source fingerprint, and archive SHA256 against <a href="./source/release.json">source/release.json</a> and <a href="./ai.json">ai.json</a>. If a cached page, mirror, or AI/search snippet shows a different combination, treat it as a stale copy.</p>
          <p data-l="en"><strong>Operator-control boundary:</strong> under the published Game client CID and a transcript that verifies locally, the operator/relay cannot forge or alter signed events, choose a target player's card, read unrevealed hole cards, or rewrite winners while keeping the verifier passing. Private dealing messages are end-to-end encrypted to the recipient, so the relay sees only ciphertext and cannot read per-card decryption keys or any hidden hole card. This is an artifact-verification claim, not a hidden-server or external-badge trust claim.</p>
        </div>
      </section>

      <div data-l="zh">
        <div class="fp-callout">
          <p><strong>核心结论：</strong>伪装浏览器、改 User-Agent、改语言/时区/IP 等，最多影响脱敏的安全提示；它不能让攻击者直接看到别人的底牌。底牌需要对应玩家浏览器中的逐牌解密钥，解密钥只在发给该玩家或公开摊牌时出现。</p>
        </div>
        <h2>攻击面与防护边界</h2>
        <div class="fp-table-wrap">
          <table>
            <thead><tr><th>攻击手段</th><th>系统防护</th><th>验证结果</th></tr></thead>
            <tbody>
              <tr><td>伪装浏览器信息、账号身份或设备环境</td><td>浏览器信息只作为脱敏安全信号，不参与发牌、解密或胜负判定。</td><td>设备信号不会给任何人增加看牌、控牌或改结果的权限。</td></tr>
              <tr><td>试图偷看别人底牌</td><td>每张牌需要多方逐步解密。私有发牌阶段的解密事件只发给对应玩家；公开牌只在翻牌、转牌、河牌或摊牌时释放。逐卡解密钥仅在当前这一手牌进行期间暂存于本机，牌局一结束即删除，不长期保留。</td><td>私有发牌消息端到端加密发给收牌方，中继只见密文、拿不到逐卡解密钥，也看不到未公开底牌；其他玩家只会收到发给自己的私有消息。</td></tr>
              <tr><td>篡改下注、弃牌、发牌或结果</td><td>事件由玩家签名，接收端校验签名、sender、payload hash；transcript 用 hash-chain 记录顺序与内容。</td><td>改事件、删事件、换顺序或改赢家，会破坏签名校验、payload hash 或 hash-chain replay。</td></tr>
              <tr><td>中继服务器偷牌或控牌</td><td>使用 Cloudflare Worker 作为轻量 WebSocket 边缘中继，没有传统发牌服务器或自建 VPS 后端；中继只转发签名消息，不充当发牌方，不持有玩家签名私钥或账号密钥库。私有发牌消息端到端加密。</td><td>中继不能伪造或篡改签名事件，也不能像中心化 RNG 服务器一样单方决定牌序；私有发牌端到端加密，中继看不到逐卡解密钥与未公开底牌。</td></tr>
              <tr><td>账号密码或本地密钥被盗</td><td>账号密钥库用密码派生密钥本地加密；登录改发客户端派生的 authSecret，服务端只存其哈希、不接触明文密码。</td><td>账号安全与发牌公平分层处理；身份恢复材料不参与牌序生成或赢家计算。</td></tr>
              <tr><td>玩家串通、共享屏幕或主动报牌</td><td>系统可显示脱敏的伙牌安全信号与同网络/同设备线索。</td><td>这类线下行为不改变"服务器不发牌、transcript 可复验"的证据链——但密码学也无法消灭线下串通（如实披露）。</td></tr>
              <tr><td>替换前端代码或假冒官网</td><td>正式牌局客户端以 IPFS CID 固定；公开源码包有 SHA256 与 source fingerprint，可与官方发布身份比对。</td><td>官方 CID、源码包 SHA256 与 source fingerprint 提供可比对的发布身份。</td></tr>
            </tbody>
          </table>
        </div>
        <h2>用户建议</h2>
        <ul class="fp-tips">
          <li>只从 <code>fairpoker.app</code> 或官网公布的 IPFS CID 进入牌桌。</li>
          <li>重要牌局优先注册账号、使用强密码，并避免在装有可疑扩展的浏览器里玩。</li>
          <li>下载 transcript 后，可按 <a href="./verify-guide.html">复验教程</a> 在本地亲手复验。</li>
        </ul>
      </div>

      <div data-l="en">
        <div class="fp-callout">
          <p><strong>Key conclusion:</strong> spoofing User-Agent, browser language, timezone, IP, or account/device identity can affect sanitized safety signals, but it does not grant access to other players' private cards. Hole cards require per-card decryption keys held by the relevant player's browser, released only to that player or during public reveal.</p>
        </div>
        <h2>Attack surface and defense boundary</h2>
        <div class="fp-table-wrap">
          <table>
            <thead><tr><th>Attack</th><th>Defense</th><th>Verification result</th></tr></thead>
            <tbody>
              <tr><td>Browser or device spoofing</td><td>Environment data is only a sanitized safety signal; it does not drive dealing, decryption, or results.</td><td>Device signals do not grant card visibility, deck control, or result-editing power.</td></tr>
              <tr><td>Trying to see another player's cards</td><td>Cards are revealed through multi-step per-card decryption. Private decrypt events go only to the intended player until public reveal. Per-card decrypt keys are kept on your device only for the duration of the current hand and are erased when it ends; never retained long-term.</td><td>Private dealing messages are end-to-end encrypted to the recipient — the relay sees only ciphertext and gets neither per-card keys nor any unrevealed hole card. Other players only receive messages addressed to them.</td></tr>
              <tr><td>Tampering with actions or results</td><td>Player events are signed and checked against signature, sender, and payload hash. The transcript hash-chain preserves order and content.</td><td>Editing, deleting, reordering events, or changing winners breaks signature, payload-hash, or hash-chain replay checks.</td></tr>
              <tr><td>Relay server attack</td><td>Fair Poker uses a Cloudflare Worker as a lightweight WebSocket edge relay — not a traditional dealing server or self-hosted VPS backend. It forwards signed messages and holds no plaintext deck, player private keys, or full decrypt keys. Private dealing is end-to-end encrypted.</td><td>The relay cannot forge or alter signed events, nor act like a centralized RNG dealer that chooses the card order; it sees neither per-card keys nor unrevealed hole cards.</td></tr>
              <tr><td>Account password or local key theft</td><td>The vault is encrypted locally with a password-derived key; login sends a client-derived authSecret, and the server stores only its hash and never touches the plaintext password.</td><td>Account security is layered separately from deck generation and winner computation; recovery material does not feed card order or results.</td></tr>
              <tr><td>Collusion, screen sharing, or telling cards</td><td>The system can surface sanitized collusion signals and same-network / same-device hints.</td><td>This out-of-band behavior does not change the server-not-dealer, replayable-transcript evidence chain — but cryptography also cannot eliminate off-table collusion (disclosed plainly).</td></tr>
              <tr><td>Fake frontend or code replacement</td><td>The table client is pinned by IPFS CID; the source archive SHA256 and source fingerprint can be compared against the official release identity.</td><td>Published CIDs, archive SHA256, and source fingerprint give the release identity a public comparison target.</td></tr>
            </tbody>
          </table>
        </div>
        <h2>Recommendations</h2>
        <ul class="fp-tips">
          <li>Only enter tables from <code>fairpoker.app</code> or an IPFS CID published by the official site.</li>
          <li>For important games, prefer a registered account with a strong password, and avoid playing in a browser with suspicious extensions.</li>
          <li>After downloading a transcript, verify it yourself locally with the <a href="./verify-guide.html">verification guide</a>.</li>
        </ul>
      </div>
    </main>

    <footer class="fp-foot">
      <div class="fp-foot-links">
        <a href="https://fairpoker.app/"><span data-l="zh">首页</span><span data-l="en">Home</span></a>
        <a href="https://fairpoker.app/audit-report.html"><span data-l="zh">公平证据</span><span data-l="en">Evidence</span></a>
        <a href="https://fairpoker.app/verify-guide.html"><span data-l="zh">复验教程</span><span data-l="en">Verify</span></a>
        <a href="https://fairpoker.app/security.html"><span data-l="zh">安全</span><span data-l="en">Security</span></a>
        <a href="https://fairpoker.app/roadmap.html"><span data-l="zh">路线图</span><span data-l="en">Roadmap</span></a>
        <a href="https://fairpoker.app/privacy.html"><span data-l="zh">隐私</span><span data-l="en">Privacy</span></a>
        <a href="https://fairpoker.app/terms.html"><span data-l="zh">条款</span><span data-l="en">Terms</span></a>
        <a href="https://fairpoker.app/cookies.html">Cookie</a>
        <a href="https://fairpoker.app/responsible-play.html"><span data-l="zh">负责任游戏</span><span data-l="en">Responsible play</span></a>
        <a href="mailto:support@fairpoker.app">support@fairpoker.app</a>
      </div>
      <div><span data-l="zh">仅娱乐筹码 · 非真钱赌博</span><span data-l="en">Play chips only &middot; no real-money gambling</span></div>
    </footer>

    <script>
      (function(){
        var KEY='fairpoker:language';
        var avail=['zh','en'];
        function has(l){return avail.indexOf(l)>=0}
        var cur=null;try{cur=localStorage.getItem(KEY)}catch(e){}
        if(!cur||!has(cur)) cur='zh';
        document.documentElement.setAttribute('data-lang',cur);
        var bar=document.getElementById('fp-langs');
        if(bar){
          var btns=bar.querySelectorAll('button');
          Array.prototype.forEach.call(btns,function(b){
            var l=b.getAttribute('data-set');
            b.setAttribute('aria-pressed', l===cur?'true':'false');
            b.addEventListener('click',function(){
              document.documentElement.setAttribute('data-lang',l);
              try{localStorage.setItem(KEY,l)}catch(e){}
              Array.prototype.forEach.call(btns,function(x){x.setAttribute('aria-pressed', x.getAttribute('data-set')===l?'true':'false')});
            });
          });
        }
      })();
    </script>
    <script src="./release-identity-loader.js" defer></script>
  </body>
</html>
